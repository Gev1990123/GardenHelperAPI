services:
  - type: web
    name: GardenHelper-API
    env: python
    region: frankfurt
    plan: free
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: port
        value: 10000